variables:
  stream: rawhide
  prod: false

metadata:
  # tell `cosa build` to default to buildah path
  build_with_buildah: true
  # tell `cosa osbuild` to use bootc-install manifests
  use_bootc_install: true

releasever: 44

repos:
  - fedora-rawhide

include: manifests/fedora-coreos.yaml
